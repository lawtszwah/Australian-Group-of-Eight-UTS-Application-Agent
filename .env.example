# 复制成 .env 再填真实密钥： cp .env.example .env
# .env 已在 .gitignore 里，不会被提交

# DeepSeek（默认供应商）。密钥从 https://platform.deepseek.com 获取
DEEPSEEK_API_KEY = sk-67c67cd5520b43eeb4d1ee109bc55198

# Anthropic（可选，用于模型对比评估）
# ANTHROPIC_API_KEY=sk-ant-xxx

# 默认供应商：deepseek 或 anthropic
# GO8_PROVIDER=deepseek
